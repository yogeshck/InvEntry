<UserControl x:Class="InvEntry.Views.GenericReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:InvEntry.Views"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:behavior="clr-namespace:InvEntry.Behavior"
             xmlns:viewmodels="clr-namespace:InvEntry.ViewModels"
             xmlns:ext="clr-namespace:InvEntry.Extension"
             d:DataContext="{d:DesignInstance Type=viewmodels:GenericReportViewModel}"
             DataContext="{ext:DISource Type=viewmodels:GenericReportViewModel}"
             mc:Ignorable="d"
             HorizontalAlignment="Left"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ComboBox for selecting type -->
        <dxe:ComboBoxEdit ItemsSource="{Binding ReportNameList}"
                          EditValue="{Binding SelectedType, UpdateSourceTrigger=PropertyChanged}"
                          IsTextEditable="False"
                          Width="200"
                          Height="35"
                          Margin="10"
                          FontSize="15" />

        <dxg:GridControl x:Name="gridControl"
                         Grid.Row="1"
                         ItemsSource="{Binding GenericItems.DefaultView}"
                         AutoGenerateColumns="AddNew"
                         Width="1200"
                         Height="400"
                         Margin="10">
            <dxg:GridControl.View>
                <dxg:TableView ShowGroupPanel="False" />
            </dxg:GridControl.View>
        </dxg:GridControl>


    </Grid>
</UserControl>
