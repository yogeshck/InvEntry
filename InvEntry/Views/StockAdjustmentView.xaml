<UserControl x:Class="InvEntry.Views.StockAdjustmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InvEntry.Views"
             xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Stock Grid -->
            <dxg:GridControl ItemsSource="{Binding ProductStocks}" Grid.Row="0">
                <dxg:GridControl.View>
                    <dxg:TableView AutoWidth="True" ShowGroupPanel="False"/>
                </dxg:GridControl.View>
                <dxg:GridColumn FieldName="ProductName" Header="Product"/>
                <dxg:GridColumn FieldName="Code" Header="Code"/>
                <dxg:GridColumn FieldName="Sublocation" Header="Sublocation"/>
                <dxg:GridColumn FieldName="Initial" Header="Initial"/>
                <dxg:GridColumn FieldName="Change" Header="Change"/>
                <dxg:GridColumn FieldName="Final" Header="Final"
                        UnboundType="Integer"
                        UnboundExpression="[Initial] + [Change]"/>
            </dxg:GridControl>

            <!-- Action Buttons 
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10" Grid.Row="1">
            <dxe:SimpleButton Content="Remove stock" Command="{Binding RemoveStockCommand}" Margin="5"/>
            <dxe:SimpleButton Content="Add stock" Command="{Binding AddStockCommand}" Margin="5"/>
            <dxe:SimpleButton Content="Adjust stock" Command="{Binding AdjustStockCommand}" Margin="5"/>
        </StackPanel>


         Action Buttons 
        <StackPanel Orientation="Horizontal" Margin="10">
            <dx:SimpleButton Content="Apply Adjustment"
                      Command="{Binding ApplyAdjustmentCommand}"
                      IsEnabled="{Binding CanApplyAdjustment}" />
            <dx:SimpleButton Content="Cancel"
                      Command="{Binding CancelCommand}" />
        </StackPanel>  -->
    </Grid>
</UserControl>
