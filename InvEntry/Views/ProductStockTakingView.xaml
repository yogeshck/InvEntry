<UserControl x:Class="InvEntry.Views.ProductStockTakingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InvEntry.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Background="#D84315" Padding="15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Stock Taking"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="White"/>
                <TextBlock Text="   (Manual Scanner / Typing Supported)"
                           FontSize="14"
                           Foreground="White"
                           VerticalAlignment="Bottom"/>
            </StackPanel>
        </Border>


        <!-- SCAN SECTION -->
        <Border Grid.Row="1"
                Margin="20"
                Padding="20"
                Background="White"
                CornerRadius="5"
                BorderBrush="#DDD"
                BorderThickness="1">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <!-- Barcode Input -->
                <StackPanel>

                    <TextBlock Text="Scan / Enter Barcode"
                               FontSize="16"
                               FontWeight="SemiBold"/>

                    <TextBox x:Name="txtBarcode"
                             Height="40"
                             FontSize="18"
                             Margin="0,10,0,0"
                             VerticalContentAlignment="Center"
                             HorizontalContentAlignment="Left"
                             FocusManager.FocusedElement="{Binding RelativeSource={RelativeSource Self}}" />
                    <!-- KeyDown="txtBarcode_KeyDown"/>  -->

                    <TextBlock Text="Tip: USB scanner works like keyboard. After scan, press Enter."
                               Foreground="Gray"
                               Margin="0,8,0,0"/>
                </StackPanel>

                <!-- Summary Panel -->
                <Border Grid.Column="1"
                        Background="#FFF3E0"
                        Padding="15"
                        CornerRadius="5"
                        Margin="20,0,0,0">

                    <StackPanel>
                        <TextBlock Text="Session Summary"
                                   FontSize="16"
                                   FontWeight="Bold"/>

                        <TextBlock Text="Total Scanned"
                                   Margin="0,10,0,0"
                                   FontWeight="SemiBold"/>

                        <TextBlock Text="{Binding TotalScanned}"
                                   FontSize="28"
                                   Foreground="#2E7D32"
                                   FontWeight="Bold"/>

                        <Button Content="Complete Session"
                                Margin="0,20,0,0"
                                Height="40"
                                Background="#2E7D32"
                                Foreground="White"
                                Command="{Binding CompleteCommand}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- SCANNED LIST -->
        <Border Grid.Row="2"
                Margin="20"
                Padding="15"
                Background="White"
                CornerRadius="5"
                BorderBrush="#DDD"
                BorderThickness="1">

            <StackPanel>
                <TextBlock Text="Last Scanned Items"
                           FontSize="16"
                           FontWeight="SemiBold"/>

                <DataGrid ItemsSource="{Binding RecentScanned}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          Margin="0,10,0,0">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Barcode"
                                            Binding="{Binding Barcode}"
                                            Width="2*"/>

                        <DataGridTextColumn Header="Scan Time"
                                            Binding="{Binding ScanTime}"
                                            Width="*"/>

                        <DataGridTextColumn Header="Status"
                                            Binding="{Binding Status}"
                                            Width="*"/>
                    </DataGrid.Columns>

                </DataGrid>
            </StackPanel>
        </Border>
        
        

    </Grid>
</UserControl>
