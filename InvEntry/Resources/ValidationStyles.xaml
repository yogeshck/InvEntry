<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <!-- ============================================================
         VALIDATION COLOURS

         Centralized here so individual screens never define them.
         ============================================================ -->

    <SolidColorBrush x:Key="AppValidationBorderBrush"
                     Color="#E5A3A3"/>

    <SolidColorBrush x:Key="AppValidationBackgroundBrush"
                     Color="#22D92D20"/>

    <SolidColorBrush x:Key="AppValidationForegroundBrush"
                     Color="#D92D20"/>


    <!-- ============================================================
         VALIDATION PANEL
         ============================================================ -->

    <Style x:Key="AppValidationPanelStyle"
           TargetType="Border">

        <Setter Property="Margin"
                Value="12,4,12,4"/>

        <Setter Property="Padding"
                Value="12,8"/>

        <Setter Property="CornerRadius"
                Value="6"/>

        <Setter Property="BorderThickness"
                Value="1"/>

        <Setter Property="BorderBrush"
                Value="{StaticResource AppValidationBorderBrush}"/>

        <Setter Property="Background"
                Value="{StaticResource AppValidationBackgroundBrush}"/>

    </Style>


    <!-- ============================================================
         VALIDATION TITLE
         ============================================================ -->

    <Style x:Key="AppValidationTitleStyle"
           TargetType="TextBlock">

        <Setter Property="Foreground"
                Value="{StaticResource AppValidationForegroundBrush}"/>

        <Setter Property="FontWeight"
                Value="Bold"/>

        <Setter Property="FontSize"
                Value="14"/>

        <Setter Property="Margin"
                Value="0,0,0,4"/>

    </Style>


    <!-- ============================================================
         VALIDATION MESSAGE
         ============================================================ -->

    <Style x:Key="AppValidationMessageStyle"
           TargetType="TextBlock">

        <Setter Property="Foreground"
                Value="{StaticResource AppValidationForegroundBrush}"/>

        <Setter Property="FontSize"
                Value="14"/>

        <Setter Property="FontWeight"
                Value="SemiBold"/>

        <Setter Property="TextWrapping"
                Value="Wrap"/>

        <Setter Property="Margin"
                Value="0,2,0,2"/>

    </Style>

</ResourceDictionary>